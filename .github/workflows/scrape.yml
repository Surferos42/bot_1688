name: 1688 bot

on:
  workflow_dispatch:   # uruchamiasz ręcznie
  schedule:
    - cron: "0 */12 * * *"   # co 12h (opcjonalnie)

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          playwright install chromium

      - name: 🕷 Run scraper
        run: |
          python scraper.py
